package objetos;

public interface Mascota {  // en la interface solamente se pone la cabecera del metodo, es como una plantilla con metodos, y las clases que implementan esta interface heredan estos metodos pero tiene que terminar de definirlos
	
	public void corre();
	public int duplicaHorasPaseo(int horas);
	
	// en la interface solamente se pone la cabecera del metodo
		

}
